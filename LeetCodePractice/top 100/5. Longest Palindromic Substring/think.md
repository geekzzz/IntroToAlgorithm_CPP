中心扩展法
      假设一个字符串str是回文串，那么它一定是左右对称的，所以该字符串以某个字符str[mid]为中心的前缀和后缀一定是相同的。例如，对于回文串"aba"，以'b'为中心，它的前缀和后缀都是'a'。故，可以枚举字符串的每个字符作为回文串的中心位置，左右扩展得到回文串，然后比较得到最长的长度。

注意：这里需要考虑到一个特殊情况，即存在"YXaaXY"这样长度为偶数的特殊子串，这里可以将"aa"视为'a'。在本题的解法中，用了一个while（s[k] == s[k + 1]）{++k},来跳过了麻烦的检测过程。


参考：https://www.cnblogs.com/xiuyangleiasp/p/5070991.html